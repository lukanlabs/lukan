[plugin]
name = "gmail"
version = "0.1.0"
alias = "gmail"
description = "Gmail tools (search, read, send, reply, modify)"
plugin_type = "tools"

[auth]
type = "token"
check_field = "accessToken"

[config.clientId]
type = "string"
description = "Google OAuth Client ID"

[config.clientSecret]
type = "string"
description = "Google OAuth Client Secret"

[config.accessToken]
type = "string"
description = "OAuth access token (set by auth)"

[config.refreshToken]
type = "string"
description = "OAuth refresh token (set by auth)"

[config.tokenExpiry]
type = "number"
description = "Token expiry timestamp in ms (set by auth)"

[commands.auth]
description = "Authenticate with Google for Gmail access (OAuth2 PKCE)"
handler = "auth"
