[
  {
    "name": "WhatsAppSend",
    "description": "Send a text message to any WhatsApp contact or group. Use phone number (e.g. '5511999999999') for contacts or a JID (e.g. '120363...@g.us') for groups.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "to": {
          "type": "string",
          "description": "Phone number or JID of the recipient"
        },
        "text": {
          "type": "string",
          "description": "Message text to send"
        }
      },
      "required": ["to", "text"]
    }
  },
  {
    "name": "WhatsAppSendMedia",
    "description": "Send a media file (image, document, audio, video) to a WhatsApp contact or group.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "to": {
          "type": "string",
          "description": "Phone number or JID of the recipient"
        },
        "filePath": {
          "type": "string",
          "description": "Absolute path to the media file"
        },
        "mediaType": {
          "type": "string",
          "description": "Type of media: image, document, audio, or video",
          "enum": ["image", "document", "audio", "video"]
        },
        "caption": {
          "type": "string",
          "description": "Optional caption for the media"
        },
        "fileName": {
          "type": "string",
          "description": "File name for documents (defaults to original file name)"
        }
      },
      "required": ["to", "filePath", "mediaType"]
    }
  },
  {
    "name": "WhatsAppListChats",
    "description": "List recent WhatsApp chats with last message preview. Shows contacts and groups the connector has seen messages from.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "number",
          "description": "Maximum number of chats to return (default: 20)"
        }
      },
      "required": []
    }
  },
  {
    "name": "WhatsAppReadMessages",
    "description": "Read recent messages from a specific WhatsApp chat. Returns message history with sender, text, and timestamp.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "chatId": {
          "type": "string",
          "description": "Chat JID (e.g. '5511999999999@s.whatsapp.net' or group JID)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of messages to return (default: 20)"
        }
      },
      "required": ["chatId"]
    }
  },
  {
    "name": "WhatsAppSearchContacts",
    "description": "Search WhatsApp contacts and groups by name. Returns matching JIDs that can be used with WhatsAppSend.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query to match against contact/group names"
        }
      },
      "required": ["query"]
    }
  }
]
